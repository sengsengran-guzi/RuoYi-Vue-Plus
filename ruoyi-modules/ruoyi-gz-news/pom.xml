<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>org.dromara</groupId>
        <artifactId>ruoyi-modules</artifactId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>ruoyi-gz-news</artifactId>

    <description>
        谷子宇宙资讯 CMS：文章 / 分类 / 定时发布
        D01 GZ-SYS-001 阶段仅 HelloController 占位
    </description>

    <dependencies>
        <dependency>
            <groupId>org.dromara</groupId>
            <artifactId>ruoyi-gz-common</artifactId>
            <version>${revision}</version>
        </dependency>

        <!-- GZ-NEWS-003 富文本 XSS 白名单清洗：jsoup Safelist.relaxed + 额外去危险节点（ticket AC4 / D2） -->
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
            <version>1.17.2</version>
        </dependency>

        <!-- GZ-NEWS-004 定时发布 cron：SnailJob 执行器（@JobExecutor / JobArgs / ExecuteResult / SnailJobLog） -->
        <dependency>
            <groupId>org.dromara</groupId>
            <artifactId>ruoyi-common-job</artifactId>
        </dependency>

        <!-- GZ-NEWS-001 单元测试（Mockito + JUnit 5），与 ruoyi-gz-bean 同款 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

</project>
