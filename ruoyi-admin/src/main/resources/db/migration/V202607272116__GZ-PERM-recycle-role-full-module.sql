-- ============================================================
-- GZ-PERM 回收店员(role 103) 放开为「回收板块全权限」（客户 2026-07-27）
--
-- 原口径：只给回收看板的查询 + 到店核对（13002/13020/13021/13040），配置类与打款/取消留 owner。
-- 新口径（客户）：回收店员应拥有**回收板块的所有权限**，不止看板。
--
-- 做法：把 GZ-RECYCLE 整段（menu_id 13000-13999，CLAUDE.md §6 菜单分段）全部授予 role 103。
--   用范围 SELECT 而非逐条 INSERT —— 以后回收域新增菜单，重跑本段即可对齐（本迁移本身 append-only 不改）。
--
-- 授权后回收店员新获得的能力（相对原口径）：
--   · 13007 回收配置（C，会出现在侧边栏）→ 数量桶时长 13040-13043 / 回收时段 13050-13053 增删改
--   · 13022 触发打款  gz:recycle:appointment:payout  ← ⚠️ 原为 owner 兜底权（涉及真实出账）
--   · 13023 预约取消  gz:recycle:appointment:cancel  ← ⚠️ 原为 owner 兜底权
--   · 13001/13010-13014 回收价目表（该页 visible='1' 已隐藏，GZ-RECYCLE-007 去估价后停用，授了不显示）
--   侧边栏变化：仅新增「回收配置」一项（13001/13005/13006 visible='1' 隐藏，不进侧边栏；
--   F 类型本就不进侧边栏 —— 见 SysMenuServiceImpl.selectMenuTreeByUserId 只取 M/C）。
--
-- 有意不授（跨域，不属「回收板块」）：
--   · 11006/11061/11062「回收站」gz:recycle:bin:* —— 这是**管理后台数据回收站**（删除恢复/归档），
--     与回收业务同名不同物，属 GZ-ADMIN 段，别混
--   · 5100 段 反向打款单管理（属 GZ-PAY 支付域）
--   · 门店/文件等支撑读权限已由 V202607272105 单独授（6002/5032/5033），不在本段
--
-- ⚠️ 授权后账号需重登才生效（权限在登录那刻冻结进 LoginUser）。
-- ============================================================

SET NAMES utf8mb4;

-- 回收店员(103) ← GZ-RECYCLE 整段（13000-13999，含目录/页面/按钮全部类型）
INSERT IGNORE INTO sys_role_menu (role_id, menu_id)
SELECT 103, menu_id FROM sys_menu WHERE menu_id BETWEEN 13000 AND 13999;
