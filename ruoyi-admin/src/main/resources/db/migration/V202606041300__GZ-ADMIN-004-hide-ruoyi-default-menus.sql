-- ============================================================
-- GZ-ADMIN-004 隐藏与谷子业务无关的 ruoyi 自带默认顶级菜单 + 重复的数据看板空壳。
--
-- 谷子后台只暴露：系统管理（用户/角色/菜单/字典/文件等，menu_id=1）、
--   数据看板（GZ-ADMIN-003 真页 menu_id=300）、谷子业务（5000）、支付管理（5100）。
--
-- 隐藏机制：sys_menu.visible（'0'=显示 / '1'=隐藏）。用 visible 而非 status，
--   保留权限与可管理性 —— superadmin 在「系统管理 → 菜单管理」里仍能看到并按需放开。
--
-- 隐藏清单：
--   2     系统监控（ruoyi 自带，在线用户/缓存/任务调度 — 谷子用 SnailJob 独立控制台，不走这里）
--   3     系统工具（ruoyi 自带，代码生成器）
--   4     PLUS官网（ruoyi 自带外链）
--   5     测试菜单（ruoyi 自带演示）
--   6     租户管理（ruoyi 自带，谷子 V1.0 单租户 '1001'，无需暴露）
--   5500  数据看板（GZ-ADMIN-004 预留的空目录壳，已被 ADMIN-003 的真页 menu_id=300 取代，侧边栏重复）
--   11616 工作流（ruoyi-workflow 自带）
--   11618 我的任务（ruoyi-workflow 自带）
-- ============================================================

SET NAMES utf8mb4;

UPDATE sys_menu SET visible = '1'
WHERE menu_id IN (2, 3, 4, 5, 6, 5500, 11616, 11618);
